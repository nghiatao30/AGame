/// <summary>
/// Define all type of Source/Sink location in game.
/// *NOTE: ResourceLocation needs to be defined in GameAnalytics settings (ResourceItemTypes section) in order to work properly.
/// </summary>
public enum ResourceLocation
{
    // Source & Sink
    None,
    FreeMoney,
    MoneyPack,
    GemPack,
    PvP,
    Box,
    BoosterPack,
    Upgrade,
    TrophyRoad,
}